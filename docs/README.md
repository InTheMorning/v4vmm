# Documentation

This repo now documents the current desktop app instead of older roadmap notes.

Start here:

- [App overview](app-overview.md): tabs, dependencies, config, and the main operator model
- [Library and discovery workflows](workflows.md): what subscribing, caching, compare, and feed refresh actually do
- [Storage and metadata model](storage-and-metadata.md): SQLite tables, file layout, metadata sources, and current limits

Historical architecture decisions still live in [`docs/adr/`](adr/).
